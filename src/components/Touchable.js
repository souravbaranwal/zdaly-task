import {space, color, border, position, layout, flexbox} from 'styled-system';
import styled from 'styled-components';

export const Touchable = styled.TouchableOpacity`
  ${space}
  ${color}
  ${border}
  ${position}
  ${layout}
  ${flexbox}
`;
