import React from 'react';

import {Container, Typography} from '../components';

export const Details = () => {
  return (
    <Container>
      <Typography>Details Screen</Typography>
    </Container>
  );
};
