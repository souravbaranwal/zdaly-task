import React from 'react';

import {Container, Typography} from '../components';

export const Stations = () => {
  return (
    <Container>
      <Typography>Stations Screen</Typography>
    </Container>
  );
};
